# Login ETMENEZES (responsável / Eliza) — credenciais em flows/.env
#
# Papel fixo RESPONSAVEIS — NÃO chamar garantir_perfil_* / selecionar_funcao.
# NOME_USUARIO: Eliza e/ou filhos na home.

appId: br.com.polygonus.mobile.amostra
---
- runFlow: assert_entrar_screen.yaml

- runFlow:
    file: login_as.yaml
    env:
      LOGIN: ${LOGIN_ETMENEZES}
      SENHA: ${SENHA}
      NOME_USUARIO: "Eliza|Teixeira|Bruno|Davi|Ana|.*MURAL.*"
