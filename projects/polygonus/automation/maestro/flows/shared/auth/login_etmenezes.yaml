# Login ETMENEZES (responsável) — credenciais em flows/.env

appId: br.com.polygonus.mobile.amostra
---
- runFlow: ensure_login_screen.yaml

- runFlow:
    file: login_as.yaml
    env:
      LOGIN: ${LOGIN_ETMENEZES}
      SENHA: ${SENHA}
