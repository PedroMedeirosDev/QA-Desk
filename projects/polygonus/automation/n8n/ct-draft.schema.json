{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://polygonus-qa.local/schemas/ct-draft.schema.json",
  "title": "CtDraftFields",
  "description": "Metadados de caso de teste gerados por IA/N8N. description ≠ preconditions ≠ expectedResult.",
  "type": "object",
  "additionalProperties": false,
  "required": ["title", "description", "preconditions", "expectedResult", "steps"],
  "properties": {
    "title": {
      "type": "string",
      "minLength": 3,
      "description": "Nome curto do CT"
    },
    "description": {
      "type": "string",
      "minLength": 10,
      "description": "Objetivo do teste. NUNCA incluir a palavra Pré-requisito (usar campo preconditions)."
    },
    "preconditions": {
      "type": "string",
      "minLength": 5,
      "description": "Estado necessário antes de executar (dados, perfil, fixtures)"
    },
    "expectedResult": {
      "type": "string",
      "minLength": 5,
      "description": "O que deve ser verdade ao final do CT"
    },
    "steps": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 3 },
      "description": "Passos humanos sem numeração obrigatória"
    },
    "flowPath": {
      "type": "string",
      "description": "Path relativo do flow Maestro no monorepo"
    },
    "module": {
      "type": "string",
      "description": "Módulo do produto (ex.: Mural)"
    }
  }
}
